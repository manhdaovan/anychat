<% content_for :title, 'Welcome to your chat!' %>
<div class="container">
  <div id="login-overlay" class="text-center">
    <div class="l-modal-content">
      <div class="l-modal-header">
        <h4 class="l-modal-title text-info" id="myModalLabel">Login/Register to
          <b><a href="<%= root_path %>" style="text-decoration: underline;">anychat.4me.tips</a></b>
        </h4>
      </div>
      <div class="l-modal-body">
        <div class="row">
          <div class="col-lg-6">
            <div class="well">
              <%= form_for @user, url: login_home_index_path, method: :post do |f| %>
                <div class="form-group">
                  <div class="row">
                    <div class="radio col-md-6 col-12">
                      <label>
                        <%= f.radio_button :is_new, 0, checked: true %>
                        I am a returning user.
                      </label>
                    </div>
                    <div class="radio col-md-6 col-12">
                      <label>
                        <%= f.radio_button :is_new, 1 %>
                        I am a new user.
                      </label>
                    </div>
                  </div>
                </div>
                <div class="form-group">
                  <label for="username" class="control-label"><span class="text-danger">※</span> 6 <= username.length <= 50</label>

                  <div class="notice hide">Pick any [a-zA-Z0-9] nickname!</div>
                  <%= f.text_field :username, class: 'form-control',
                                   title:            'Please enter your username',
                                   placeholder:      'Username' %>
                </div>
                <div class="form-group">
                  <label for="password" class="control-label"><span class="text-danger">※</span> 8 <= password.length <= 50</label>

                  <div class="notice hide">Pls
                    <span class="text-danger">REMEMBER</span>
                    this. We have <span class="text-danger">NO RECOVERY</span>
                    password mode!
                  </div>
                  <%= f.text_field :password, type: 'password', class: 'form-control',
                                   placeholder:     'Password',
                                   title:           'Please enter your password' %>
                </div>
                <div class="form-group hide" id="input-confirm-password">
                  <div class="notice hide"><span class="text-danger">※</span> Confirmation password</div>
                  <%= f.text_field :password_confirmation, type: 'password', class: 'form-control',
                                   placeholder:     'Confirmation password',
                                   title:           'Please enter your password again' %>
                </div>

                <%= render 'shared/error_explain', obj: @user %>

                <button type="submit" value="login" name="submit" class="btn btn-success btn-block">Login / Register
                </button>
              <% end %>
            </div>
          </div>
          <div class="col-lg-6">
            <p class="lead"><span class="text-success">FREE</span> and always be!</p>
            <ul class="list-unstyled" style="line-height: 2">
              <li><span class="fa fa-check text-success"></span> Nothing is related to your social account</li>
              <li><span class="fa fa-check text-success"></span> Nothing is related to your private info</li>
              <li><span class="fa fa-check text-success"></span> Nothing is related to data mining or something like
                that
              </li>
              <li><span class="fa fa-check text-success"></span> Nothing is saved after you quit</li>
              <li><span class="fa fa-check text-success"></span> Just <span class="text-success">private</span></li>
            </ul>
            <p>
              <button type="submit" value="login" name="submit" class="btn btn-success btn-block">Sound good! Login /
                Register now!
              </button>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript">
  $('input[name="user[is_new]"]').click(function(){
    $('#input-confirm-password, .notice').toggleClass('hide');
  });
  <% if @user.type_register? %>
  $('#user_is_new_1').trigger('click');
  <% end %>
</script>